name: mis-analytics-service
services:
  - name: web
    image:
      registry: ghcr.io
      repository: ${{ github.repository }}
      tag: latest
      registry_type: DOCR
    run_command: "npm start"
    http_port: 3000
    envs:
      - key: PORT
        value: "3000"
      - key: MONGODB_URI
        value: "${MONGODB_URI}"
      - key: JWT_SECRET
        value: "${JWT_SECRET}"
      - key: ALLOWED_ORIGINS
        value: "${ALLOWED_ORIGINS}"
    routes:
      - path: /
        preserve_path: true
        type: default
